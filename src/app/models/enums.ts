export enum Genders {
  male,
  female,
  other
}

export enum Roles {
  champion,
  challenger
}

export enum Category{
  junior,
  Senior,
  Veteran
}

export enum Status {
  pending,
  inProgress,
  finished,
}
